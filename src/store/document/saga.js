import { takeEvery } from "redux-saga/effects"

// function* getSenderDetails(payload) {
//     yield console.log(payload);
// }

export default function* documentSaga() {
    // yield takeEvery("doc/senderDetails", getSenderDetails)
}